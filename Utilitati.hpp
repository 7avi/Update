// Utilitati.hpp
#pragma once
#include <iostream>
#include <vector>

class Utilitati {
public:
    static void afisareMesajPersonalizat(const std::string& mesaj);
    static double calculMedie(const std::vector<int>& numere);
};
